import React from 'react'
import Cards from '../Cards/Cards'

const Home = ({props}) => {

  return (
    <div>
      <Cards props={props}/>
      <button></button>
      <button></button>
    </div>
  )
}

export default Home;
